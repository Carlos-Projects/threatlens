schema: carlos-harness/v1
project:
  name: ThreatLens
  owner: Carlos Rocha
  status: active
  domain: ai-security-threat-intel
  summary: "Threat intelligence aggregation and correlation for the AI/MCP security ecosystem."

context_budget:
  startup_files:
    - AGENTS.md
    - HARNESS.yml
    - progress/current.md
    - docs/architecture.md
    - docs/verification.md
  max_startup_lines: 500
  prefer_search_over_bulk_read: true

agent_adapters:
  codex:
    entrypoint: AGENTS.md
    progress_file: progress/current.md
  opencode:
    config_file: opencode.jsonc
    instructions:
      - AGENTS.md
      - HARNESS.yml
      - progress/current.md
    use_skills_for_execution_not_memory: true
  claude:
    entrypoint: AGENTS.md
  cursor:
    entrypoint: AGENTS.md

commands:
  setup:
    - pip install -e ".[dev,web]"
  test:
    - python -m pytest tests/ -v
  lint:
    - ruff check src/
  typecheck:
    - mypy src/
  build:
    - hatch build
  security:
    - python -m pytest tests/ -v

governance:
  policy_file: policies/default.yaml
  audit_log: progress/audit.jsonl
  token_budget:
    max_tokens_per_session: 120000
    max_tokens_per_task: 30000
  approval_required_for:
    - secrets
    - production_deploy
    - database_migration
    - destructive_command
    - public_publish

evidence:
  required_artifacts:
    - progress/current.md
    - progress/history.md
  optional_artifacts:
    - reports/security.md
    - reports/test-results.md
    - reports/redteam.md

integrations:
  obsidian_notes:
    - "/Users/carlosrocha/Documents/Obsidian Vault/03_Projects/ThreatLens"
  github_repo: "Carlos-Projects/threatlens"
  website: ""
  package: "threatlens-ai"
